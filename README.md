My take on a command line utility to append a filename column to a delimited file written in Rust.
Requires a file path and delimiter as command line arguments.

Example usage:
Cargo run -- "C:\files\somefile.csv" ","
